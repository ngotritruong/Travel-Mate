{
  "name": "travel-mate",
  "version": "1.0.0",
  "description": "1. Clone the repository.\r 2. Install dependencies with `npm install`.",
  "nguyenthithu": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^29.5.0"
  }
}
